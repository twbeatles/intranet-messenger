window.MessengerApp = window.MessengerApp || {};
window.MessengerApp.features = window.MessengerApp.features || {};
window.MessengerApp.features.messages = {
    renderMessages: window.renderMessages,
    scrollToBottom: window.scrollToBottom,
    createMessageElement: window.createMessageElement,
    appendMessage: window.appendMessage,
    sendMessage: window.sendMessage,
    handleTyping: window.handleTyping,
    editMessage: window.editMessage,
    deleteMessage: window.deleteMessage,
    handleMessageDeleted: window.handleMessageDeleted,
    handleMessageEdited: window.handleMessageEdited,
    setReplyTo: window.setReplyTo,
    clearReply: window.clearReply,
    setReplyToFromId: window.setReplyToFromId,
    scrollToMessage: window.scrollToMessage,
    setupMention: window.setupMention,
    parseMentions: window.parseMentions,
    hideMentionAutocomplete: window.hideMentionAutocomplete,
    invalidateMentionCache: window.invalidateMentionCache,
    handleFileUpload: window.handleFileUpload,
    toggleReaction: window.toggleReaction,
    updateMessageReactions: window.updateMessageReactions,
    showReactionPicker: window.showReactionPicker,
    closeAllReactionPickers: window.closeAllReactionPickers,
    initLazyLoadMessages: window.initLazyLoadMessages,
    loadOlderMessages: window.loadOlderMessages,
    cleanupLazyDecryptObserver: window.cleanupLazyDecryptObserver,
    initEmojiPicker: window.initEmojiPicker,
    setupDragDrop: window.setupDragDrop,
    uploadFile: window.uploadFile,
    initMessageContextMenu: window.initMessageContextMenu,
    showMessageContextMenu: window.showMessageContextMenu,
    closeMessageContextMenu: window.closeMessageContextMenu,
    showSkeletonLoading: window.showSkeletonLoading,
    hideSkeletonLoading: window.hideSkeletonLoading,
    updateInputState: window.updateInputState,
    initInputEnhancements: window.initInputEnhancements,
    cleanupLazyLoadObserver: window.cleanupLazyLoadObserver,
    initMobileSwipeReply: window.initMobileSwipeReply,
    initMobileSidebar: window.initMobileSidebar,
    openMobileSidebar: window.openMobileSidebar,
    closeMobileSidebar: window.closeMobileSidebar,
    toggleMobileSidebar: window.toggleMobileSidebar
};

