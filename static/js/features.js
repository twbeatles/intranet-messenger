window.MessengerApp = window.MessengerApp || {};
window.MessengerApp.features = window.MessengerApp.features || {};
window.MessengerApp.features.chat = {
    initRoomV4Features: window.initRoomV4Features,
    openLightbox: window.openLightbox,
    closeLightbox: window.closeLightbox,
    prevImage: window.prevImage,
    nextImage: window.nextImage,
    openPollModal: window.openPollModal,
    createPoll: window.createPoll,
    votePoll: window.votePoll,
    loadRoomPolls: window.loadRoomPolls,
    closePoll: window.closePoll,
    openFilesModal: window.openFilesModal,
    loadRoomFiles: window.loadRoomFiles,
    deleteFile: window.deleteFile,
    loadPinnedMessages: window.loadPinnedMessages,
    pinCurrentMessage: window.pinCurrentMessage,
    deletePin: window.deletePin,
    checkAdminStatus: window.checkAdminStatus,
    openAdminModal: window.openAdminModal,
    toggleAdmin: window.toggleAdmin,
    openChatSearch: window.openChatSearch,
    closeChatSearch: window.closeChatSearch,
    chatSearchNext: window.chatSearchNext,
    chatSearchPrev: window.chatSearchPrev,
    openAdvancedSearch: window.openAdvancedSearch,
    doAdvancedSearch: window.doAdvancedSearch,
    goToMessage: window.goToMessage,
    retryConnection: window.retryConnection
};

