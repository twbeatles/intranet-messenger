window.MessengerApp = window.MessengerApp || {};
window.MessengerApp.core = window.MessengerApp.core || {};
window.MessengerApp.core.utils = {
    $: window.$,
    debounce: window.debounce,
    throttle: window.throttle,
    scheduleUpdate: window.scheduleUpdate,
    escapeHtml: window.escapeHtml,
    formatTime: window.formatTime,
    formatDate: window.formatDate,
    formatDateLabel: window.formatDateLabel,
    formatFullDateTime: window.formatFullDateTime,
    parseCodeBlocks: window.parseCodeBlocks,
    copyCodeBlock: window.copyCodeBlock,
    getUserColor: window.getUserColor,
    createAvatarHtml: window.createAvatarHtml,
    createSafeAvatarHtml: window.createSafeAvatarHtml,
    copyToClipboard: window.copyToClipboard,
    safeSocketEmit: window.safeSocketEmit,
    safeImagePath: window.safeImagePath,
    batchDOMUpdate: window.batchDOMUpdate,
    batchAppendElements: window.batchAppendElements,
    delegateEvent: window.delegateEvent,
    getCachedElement: window.getCachedElement,
    clearElementCache: window.clearElementCache,
    createLazyImage: window.createLazyImage,
    initLazyLoad: window.initLazyLoad,
    runWhenIdle: window.runWhenIdle,
    processInChunks: window.processInChunks
};

